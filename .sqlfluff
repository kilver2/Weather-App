[sqlfluff]
templater = dbt
dialect = sparksql
runaway_limit = 10

[sqlfluff:rules:layout.long_lines]
max_line_length = 120

[sqlfluff:templater:dbt]
project_dir = dbt/weather
profiles_dir = dbt/weather
profile = weather
target = dev